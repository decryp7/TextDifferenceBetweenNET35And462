kind: pipeline
type: exec
name: default

platform:
  os: windows

steps:
- name: compile
  commands:
    - Invoke-MsBuild -Path FormattedTextTestClient\FormattedTextTestClient.csproj -MsBuildParameters "-t:restore" -ShowBuildOutputInCurrentWindow -PassThru

